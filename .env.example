# Ambiente
# development | production
NODE_ENV=
# 0 in locale; 1 quando l’app gira per gli utenti reali su un server pubblico se si ha un reverse proxy (Nginx/Cloudflare)
TRUST_PROXY=
# host:porta in dev (es. localhost:3000) o dominio in prod (es. gonfiabili.it)
APP_HOST=

# Email (lascia vuoti in dev se non invii)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=

# Sessione
# Metti un valore LUNGO e casuale (>= 48 char) SOLO nel tuo .env locale/produzione.
SESSION_SECRET=
